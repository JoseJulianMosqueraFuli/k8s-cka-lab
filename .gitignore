# Kubernetes
kubeconfig
*.kubeconfig
.kube/

# Solutions (keep in git but remind to not peek)
# solutions/ is tracked — honor system!

# OS
.DS_Store
Thumbs.db

# Vagrant
.vagrant/

# Temp
*.tmp
*.bak
# Terraform
**/.terraform/*
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# Variables (pueden traer secretos). Se conserva el ejemplo.
*.tfvars
*.tfvars.json
!*.tfvars.example
# Planes guardados
*.tfplan
tfplan
# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# CLI config
.terraformrc
terraform.rc
# Nota: .terraform.lock.hcl se DEJA versionado a propósito (reproducibilidad).
